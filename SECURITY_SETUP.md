# 🔒 Руководство по безопасности Faceit AI Bot

## ⚠️ Критические шаги перед деплоем

### 1. Настройка переменных окружения

**ВАЖНО**: Никогда не коммитьте `.env` файл в git!

```bash
# Скопируйте файл примера
cp .env.example .env

# Отредактируйте .env и заполните все значения
nano .env  # или используйте ваш любимый редактор
```

### 2. Генерация безопасного SECRET_KEY

```bash
# Способ 1: Используя Python
python -c "import secrets; print(secrets.token_urlsafe(32))"

# Способ 2: Используя OpenSSL
openssl rand -base64 32

# Способ 3: Используя /dev/urandom (Linux/Mac)
head -c 32 /dev/urandom | base64
```

### Вставьте полученный ключ в `.env`

```env
SECRET_KEY=ваш_сгенерированный_безопасный_ключ_здесь
```

### 3. Получение API ключей

#### Faceit API Key

1. Зарегистрируйтесь на https://developers.faceit.com/
2. Создайте новое приложение
3. Скопируйте API ключ
4. Добавьте в `.env`

```env
FACEIT_API_KEY=ваш_faceit_api_ключ
```

#### Groq API Key

1. Зарегистрируйтесь на https://console.groq.com/
2. Создайте новый API ключ
3. Добавьте в `.env`

```env
GROQ_API_KEY=ваш_groq_api_ключ
```

### 4. Настройка базы данных

```env
# Используйте сильный пароль!
POSTGRES_PASSWORD=$(openssl rand -base64 20)

# Обновите DATABASE_URL
DATABASE_URL=postgresql://faceit_user:ваш_сильный_пароль@db:5432/faceit_user
```

### 5. Настройка Redis (опционально)

```env
# Для продакшена рекомендуется установить пароль
REDIS_PASSWORD=$(openssl rand -base64 20)
REDIS_URL=redis://:ваш_redis_пароль@redis:6379
```

## Чек-лист безопасности

### Перед продакшен деплоем

- ✅ SECRET_KEY изменен и безопасен (минимум 32 символа)
- ✅ Все API ключи настроены
- ✅ Пароли баз данных изменены с дефолтных
- ✅ `.env` файл добавлен в `.gitignore`
- ✅ CORS настроен только для доверенных доменов
- ✅ HTTPS сертификат установлен
- ✅ Firewall настроен (только 80, 443 порты открыты)
- ✅ Rate limiting включен
- ✅ Логирование настроено
- ✅ Мониторинг настроен (Sentry/Prometheus)

### Регулярное обслуживание

- Ротация API ключей каждые 90 дней
- Обновление зависимостей
- Проверка логов безопасности
- Резервное копирование базы данных
- Мониторинг необычной активности

## Дополнительные меры безопасности

### 1. Использование Docker Secrets (продакшен)

```yaml
# docker-compose.prod.yml
services:
  api:
    secrets:
      - faceit_api_key
      - groq_api_key
      - db_password

secrets:
  faceit_api_key:
    file: ./secrets/faceit_api_key.txt
  groq_api_key:
    file: ./secrets/groq_api_key.txt
  db_password:
    file: ./secrets/db_password.txt
```

### 2. Ограничение прав Docker контейнеров

```dockerfile
# Используйте непривилегированного пользователя
USER appuser

# Только чтение файловой системы где возможно
volumes:
  - ./src:/app/src:ro
```

### 3. Network isolation

```yaml
# docker-compose.yml
networks:
  frontend:
    driver: bridge
  backend:
    driver: bridge
    internal: true  # Изолировать backend сеть
```

### 4. Rate Limiting настройка

```python
# Рекомендуемые лимиты для продакшена
RATE_LIMIT_PER_MINUTE = 30  # запросов в минуту на IP
RATE_LIMIT_PER_HOUR = 500   # запросов в час на IP
RATE_LIMIT_PER_DAY = 5000   # запросов в день на IP
```

## Что делать при утечке секретов

### Если SECRET_KEY скомпрометирован

1. **Немедленно** сгенерируйте новый ключ
2. Обновите `.env` файл
3. Перезапустите все сервисы
4. Инвалидируйте все активные токены пользователей
5. Уведомите пользователей о необходимости повторного входа

### Если API ключи скомпрометированы

1. **Немедленно** отзовите старые ключи в консолях API
2. Сгенерируйте новые ключи
3. Обновите `.env` и перезапустите сервисы
4. Проверьте логи на необычную активность
5. Мониторьте использование новых ключей

### Если пароли БД скомпрометированы

1. Создайте нового пользователя БД с новым паролем
2. Мигрируйте права доступа
3. Обновите `.env`
4. Перезапустите сервисы
5. Удалите старого пользователя БД

## 📊 Мониторинг безопасности

### Настройка Sentry (рекомендуется)

```python
# src/server/main.py
import sentry_sdk
from sentry_sdk.integrations.fastapi import FastApiIntegration

sentry_sdk.init(
    dsn=settings.SENTRY_DSN,
    integrations=[FastApiIntegration()],
    traces_sample_rate=0.1,
    environment=settings.NODE_ENV
)
```

### Логирование критических событий

```python
# Логировать:
- Неудачные попытки входа
- Изменения настроек безопасности
- Необычные паттерны использования API
- Ошибки аутентификации
- Rate limit превышения
```

## 📞 Контакты для сообщений о безопасности

Если вы обнаружили уязвимость безопасности:

- Email: security@pattmsc.online (или ваш email)
- Не публикуйте уязвимость публично до исправления
- Мы ответим в течение 48 часов

## 📚 Дополнительные ресурсы

- [OWASP Top 10](https://owasp.org/www-project-top-ten/)
- [Docker Security Best Practices](https://docs.docker.com/engine/security/)
- [FastAPI Security](https://fastapi.tiangolo.com/tutorial/security/)
- [PostgreSQL Security](https://www.postgresql.org/docs/current/security.html)

---

**Помните**: Безопасность — это не разовая задача, а постоянный процесс! 🛡️