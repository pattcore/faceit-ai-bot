events {
    worker_connections 1024;
}

http {
    resolver 127.0.0.11 valid=30s ipv6=off;

    server_tokens off;

    merge_slashes on;

    gzip on;
    gzip_vary on;
    gzip_proxied any;
    gzip_comp_level 6;
    gzip_types text/plain text/css text/xml text/javascript application/json application/javascript application/xml+rss;

    real_ip_header CF-Connecting-IP;
    real_ip_recursive on;

    set_real_ip_from 173.245.48.0/20;
    set_real_ip_from 103.21.244.0/22;
    set_real_ip_from 103.22.200.0/22;
    set_real_ip_from 103.31.4.0/22;
    set_real_ip_from 141.101.64.0/18;
    set_real_ip_from 108.162.192.0/18;
    set_real_ip_from 190.93.240.0/20;
    set_real_ip_from 188.114.96.0/20;
    set_real_ip_from 197.234.240.0/22;
    set_real_ip_from 198.41.128.0/17;
    set_real_ip_from 162.158.0.0/15;
    set_real_ip_from 104.16.0.0/13;
    set_real_ip_from 104.24.0.0/14;
    set_real_ip_from 172.64.0.0/13;
    set_real_ip_from 131.0.72.0/22;
    set_real_ip_from 2400:cb00::/32;
    set_real_ip_from 2606:4700::/32;
    set_real_ip_from 2803:f800::/32;
    set_real_ip_from 2405:b500::/32;
    set_real_ip_from 2405:8100::/32;
    set_real_ip_from 2a06:98c0::/29;
    set_real_ip_from 2c0f:f248::/32;
    geo $realip_remote_addr $from_cf {
        default 0;
        10.0.0.0/8 1;
        172.16.0.0/12 1;
        192.168.0.0/16 1;
        127.0.0.1/32 1;
        ::1/128 1;
        173.245.48.0/20 1;
        103.21.244.0/22 1;
        103.22.200.0/22 1;
        103.31.4.0/22 1;
        141.101.64.0/18 1;
        108.162.192.0/18 1;
        190.93.240.0/20 1;
        188.114.96.0/20 1;
        197.234.240.0/22 1;
        198.41.128.0/17 1;
        162.158.0.0/15 1;
        104.16.0.0/13 1;
        104.24.0.0/14 1;
        172.64.0.0/13 1;
        131.0.72.0/22 1;
        2400:cb00::/32 1;
        2606:4700::/32 1;
        2803:f800::/32 1;
        2405:b500::/32 1;
        2405:8100::/32 1;
        2a06:98c0::/29 1;
        2c0f:f248::/32 1;
    }

    map "$from_cf:$host:$uri" $block_non_cf {
        default 0;

        # Allow ACME challenge from anywhere
        ~^0:.*:/\.well-known/acme-challenge/ 0;

        # Allow direct (non-CF) access only for uploads host and only for upload/demo paths
        ~^0:uploads\.pattmsc\.online:/api/ 0;
        ~^0:uploads\.pattmsc\.online:/demos/ 0;

        # Block everything else that is not coming from Cloudflare
        ~^0: 1;
    }

    # Rate limiting
    limit_req_zone $binary_remote_addr zone=api_limit:10m rate=10r/s;
    limit_req_zone $binary_remote_addr zone=general_limit:10m rate=30r/s;

    limit_conn_zone $binary_remote_addr zone=addr:10m;

    map $http_user_agent $bad_bot {
        default 0;
        ~*(nmap|nikto|wikto|sf|sqlmap|bsqlbf|w3af|acunetix|havij|appscan) 1;
    }

    server {
        set $k3s_ingress 192.168.0.45:30443;

        listen 80;
        server_name _;
        client_max_body_size 1G;

        if ($request_uri ~ "^//+(.*)$") {
            return 301 /$1$is_args$args;
        }

        if ($block_non_cf) {
            return 403;
        }

        # Allow only standard REST methods; block things like CONNECT/TRACE/etc.
        if ($request_method !~ ^(GET|HEAD|POST|PUT|DELETE|PATCH|OPTIONS)$) {
            return 405;
        }

        limit_conn addr 20;

        if ($bad_bot) {
            return 403;
        }

        # Security headers
        add_header X-Content-Type-Options nosniff always;
        add_header X-Frame-Options DENY always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "no-referrer-when-downgrade" always;
        add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;

        location ~ ^/(internal|debug)/ {
            return 403;
        }

        # Block obvious SSRF/proxy scan paths
        location ~* (api\.ipify\.org|\.\./) {
            return 403;
        }

        # Block obvious CMS/PHP scan paths we don't use
        location ~* ^/(wp-admin|wp-login\.php|xmlrpc\.php|wp-content|wp-includes|phpmyadmin|phpMyAdmin|pma|adminer|config\.php)$ {
            return 403;
        }

        # Block direct access to some sensitive files/dirs
        location ~* ^/(\.env|\.git|\.hg|\.svn) {
            return 403;
        }

        # API health alias so /api/health всегда работает
        location = /api/health {
            proxy_pass https://$k3s_ingress;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
        }

        # Prometheus metrics – разрешены только локальные/приватные IP (добавь свои при необходимости)

        location ^~ /api/admin/rate-limit/ {
            limit_req zone=api_limit burst=5 nodelay;

            proxy_pass https://$k3s_ingress;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;

            proxy_read_timeout 300s;
            proxy_connect_timeout 75s;
        }

        # API endpoints
        location /api/ {
            limit_req zone=api_limit burst=20 nodelay;

            proxy_pass https://$k3s_ingress;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            
            # Timeouts for long-running requests
            proxy_read_timeout 300s;
            proxy_connect_timeout 75s;
        }

        # Health check (no rate limit)
        location /health {
            proxy_pass https://$k3s_ingress/health;
            proxy_set_header Host $host;
            access_log off;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
        }

        # API docs
        location ~ ^/(docs|redoc|openapi\.json) {
            proxy_pass https://$k3s_ingress;
            proxy_set_header Host $host;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
        }

        # Flower upstream/route временно отключены (нет контейнера flower)

        # Frontend
        location = /login {
            return 301 /auth;
        }

        location = /register {
            return 301 /auth;
        }

        location = /analytics {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;
            proxy_pass https://$k3s_ingress/metrics;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        location = /analytics/ {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;
            proxy_pass https://$k3s_ingress/metrics;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }


        location = /monitoring {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;
            return 302 /monitoring/grafana/;
        }

        location = /monitoring/ {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;
            return 302 /monitoring/grafana/;
        }

        location = /monitoring/grafana {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;
            return 302 /monitoring/grafana/;
        }

        location ^~ /monitoring/grafana/ {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;

            set $grafana_upstream http://faceit_grafana:3000;
            proxy_pass $grafana_upstream;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location = /monitoring/prometheus {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;
            return 302 /monitoring/prometheus/;
        }

        location ^~ /monitoring/prometheus/ {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;

            set $prometheus_upstream http://faceit_prometheus:9090;
            proxy_pass $prometheus_upstream;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location = /monitoring/alertmanager {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;
            return 302 /monitoring/alertmanager/;
        }

        location ^~ /monitoring/alertmanager/ {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;

            set $faceit_alertmanager_upstream faceit_alertmanager:9093;

            proxy_pass http://$faceit_alertmanager_upstream;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location / {
            limit_req zone=general_limit burst=50 nodelay;

            proxy_pass https://$k3s_ingress;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            
            # WebSocket support for Next.js hot reload
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        # Static files caching
        location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
            proxy_pass https://$k3s_ingress;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            expires 30d;
            add_header Cache-Control "public, immutable";
        }
    }

    # HTTPS server for pattmsc.online
    server {
        set $k3s_ingress 192.168.0.45:30443;

        listen 443 ssl;
        http2 on;
        server_name pattmsc.online www.pattmsc.online;

        if ($block_non_cf) {
            return 403;
        }

        ssl_certificate /etc/letsencrypt/live/pattmsc.online/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/pattmsc.online/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
        ssl_prefer_server_ciphers off;

        client_max_body_size 1G;

        if ($request_uri ~ "^//+(.*)$") {
            return 301 /$1$is_args$args;
        }

        # Allow only standard REST methods; block things like CONNECT/TRACE/etc.
        if ($request_method !~ ^(GET|HEAD|POST|PUT|DELETE|PATCH|OPTIONS)$) {
            return 405;
        }

        # Security headers
        add_header X-Content-Type-Options nosniff always;
        add_header X-Frame-Options DENY always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "no-referrer-when-downgrade" always;
        add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;
        add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;

        limit_conn addr 20;

        if ($bad_bot) {
            return 403;
        }

        location ~ ^/(internal|debug)/ {
            return 403;
        }

        # Block obvious SSRF/proxy scan paths
        location ~* (api\.ipify\.org|\.\./) {
            return 403;
        }

        # Block obvious CMS/PHP scan paths we don't use
        location ~* ^/(wp-admin|wp-login\.php|xmlrpc\.php|wp-content|wp-includes|phpmyadmin|phpMyAdmin|pma|adminer|config\.php)$ {
            return 403;
        }

        # Block direct access to some sensitive files/dirs
        location ~* ^/(\.env|\.git|\.hg|\.svn) {
            return 403;
        }

        # API health alias so /api/health всегда работает
        location = /api/health {
            proxy_pass https://$k3s_ingress;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
        }

        # Prometheus metrics – разрешены только локальные/приватные IP 

        location ^~ /api/admin/rate-limit/ {
            limit_req zone=api_limit burst=5 nodelay;

            proxy_pass https://$k3s_ingress;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;

            proxy_read_timeout 300s;
            proxy_connect_timeout 75s;
        }

        # API endpoints
        location /api/ {
            limit_req zone=api_limit burst=20 nodelay;

            proxy_pass https://$k3s_ingress;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            
            # Timeouts for long-running requests
            proxy_read_timeout 300s;
            proxy_connect_timeout 75s;
        }

        # Health check (no rate limit)
        location /health {
            proxy_pass https://$k3s_ingress/health;
            proxy_set_header Host $host;
            access_log off;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
        }

        # API docs
        location ~ ^/(docs|redoc|openapi\.json) {
            proxy_pass https://$k3s_ingress;
            proxy_set_header Host $host;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
        }

        # Frontend
        location = /login {
            return 301 /auth;
        }

        location = /register {
            return 301 /auth;
        }

        location = /analytics {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;
            proxy_pass https://$k3s_ingress/metrics;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        location = /analytics/ {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;
            proxy_pass https://$k3s_ingress/metrics;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        location = /monitoring {
            return 302 /monitoring/grafana/;
        }

        location = /monitoring/ {
            return 302 /monitoring/grafana/;
        }

        location = /monitoring/grafana {
            return 302 /monitoring/grafana/;
        }

        location ^~ /monitoring/grafana/ {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;
            set $grafana_upstream http://faceit_grafana:3000;
            proxy_pass $grafana_upstream;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location = /monitoring/prometheus {
            return 302 /monitoring/prometheus/;
        }

        location ^~ /monitoring/prometheus/ {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;
            set $prometheus_upstream http://faceit_prometheus:9090;
            proxy_pass $prometheus_upstream;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location = /monitoring/alertmanager {
            return 302 /monitoring/alertmanager/;
        }

        location ^~ /monitoring/alertmanager/ {
            auth_basic "Restricted";
            auth_basic_user_file /etc/nginx/.htpasswd;
            set $alertmanager_upstream http://faceit_alertmanager:9093;
            proxy_pass $alertmanager_upstream;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
        }

        location / {
            limit_req zone=general_limit burst=50 nodelay;

            proxy_pass https://$k3s_ingress;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            
            # WebSocket support for Next.js hot reload
            proxy_http_version 1.1;
            proxy_set_header Upgrade $http_upgrade;
            proxy_set_header Connection "upgrade";
        }

        # Static files caching
        location ~* \.(jpg|jpeg|png|gif|ico|css|js|svg|woff|woff2|ttf|eot)$ {
            proxy_pass https://$k3s_ingress;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name $host;
            expires 30d;
            add_header Cache-Control "public, immutable";
        }
    }

    server {
        listen 443 ssl;
        http2 on;
        server_name uploads.pattmsc.online;

        set $k3s_ingress 192.168.0.45:30443;

        ssl_certificate /etc/letsencrypt/live/uploads.pattmsc.online/fullchain.pem;
        ssl_certificate_key /etc/letsencrypt/live/uploads.pattmsc.online/privkey.pem;
        ssl_protocols TLSv1.2 TLSv1.3;
        ssl_ciphers 'ECDHE-ECDSA-AES128-GCM-SHA256:ECDHE-RSA-AES128-GCM-SHA256:ECDHE-ECDSA-AES256-GCM-SHA384:ECDHE-RSA-AES256-GCM-SHA384';
        ssl_prefer_server_ciphers off;

        client_max_body_size 1G;

        if ($request_uri ~ "^//+(.*)$") {
            return 301 /$1$is_args$args;
        }

        # Allow only safe methods
        if ($request_method !~ ^(GET|HEAD|POST|OPTIONS)$) {
            return 405;
        }

        # Security headers (same as main HTTPS server)
        add_header X-Content-Type-Options nosniff always;
        add_header X-Frame-Options DENY always;
        add_header X-XSS-Protection "1; mode=block" always;
        add_header Referrer-Policy "no-referrer-when-downgrade" always;
        add_header Permissions-Policy "geolocation=(), microphone=(), camera=()" always;
        add_header Strict-Transport-Security "max-age=63072000; includeSubDomains; preload" always;

        limit_conn addr 10;

        location ^~ /demos/ {
            limit_except GET HEAD {
                deny all;
            }

            alias /tmp_demos/;
            default_type application/octet-stream;
            add_header Content-Disposition "attachment" always;
            add_header Cache-Control "public, max-age=31536000, immutable" always;
        }

        # Direct API access for large uploads (e.g. demo files)
        location /api/ {
            limit_req zone=api_limit burst=5 nodelay;

            proxy_buffering off;
            proxy_request_buffering off;

            proxy_pass https://192.168.0.45:30443;
            proxy_set_header Host pattmsc.online;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name pattmsc.online;

            proxy_read_timeout 600s;
            proxy_connect_timeout 75s;
        }

        location /health {
            proxy_pass https://192.168.0.45:30443;
            proxy_set_header Host pattmsc.online;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_ssl_server_name on;
            proxy_ssl_name pattmsc.online;
            access_log off;
        }
    }
}
